<div class="card-body bg-light ">
    <div class="my-4">
        <a href="{{ path('app_consultation_index') }}"class="btn btn-primary">
            <i class="fa-solid fa-arrow-left"></i>
        </a>
    </div>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-dark">{{ button_label|default('Ajouter') }}</button>
    {{ form_end(form) }}
</div>
